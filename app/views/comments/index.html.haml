%h1 Listing comments

.table-responsive
  %table.table.table-striped
    %thead
      %tr
        %th Profile
        %th Post
        %th Description
        %th
        %th
        %th

    %tbody
      - @comments.each do |comment|
        %tr
          %td= comment.profile.name
          %td= comment.post.description
          %td= comment.description
          %td= link_to 'Show', comment
          %td= link_to 'Edit', edit_comment_path(comment)
          %td= link_to 'Destroy', comment, :method => :delete, :data => { :confirm => 'Are you sure?' }

= link_to 'New Comment', new_comment_path

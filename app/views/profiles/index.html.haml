%h1 Listing profiles

.table-responsive
  %table.table.table-striped
    %thead
      %tr
        %th Name
        %th Video Url
        %th Representitive
        %th Phone
        %th Status
        %th Facebook
        %th Twitter
        %th Instagram
        %th Category
        %th User
        %th
        %th
        %th

    %tbody
      - @profiles.each do |profile|
        %tr
          %td= profile.name
          %td= profile.video_url
          %td= profile.representitive
          %td= profile.phone
          %td= profile.status
          %td= profile.facebook
          %td= profile.twitter
          %td= profile.instagram
          %td= profile.category.name unless profile.category.nil?
          %td= profile.user.email unless profile.user.nil?
          %td= link_to 'Show', profile
          %td= link_to 'Edit', edit_profile_path(profile)
          %td= link_to 'Destroy', profile, :method => :delete, :data => { :confirm => 'Are you sure?' }

= link_to 'New Profile', new_profile_path
